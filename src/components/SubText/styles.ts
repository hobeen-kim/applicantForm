import styled from "styled-components";
import { TextField as MuiTextField } from "@mui/material";

import { InputTypes } from "../../store";

export const SubTextField = styled.span`
  font-size: 14px;
  padding-left: 3px;
  margin-bottom: 16px;
  margin-top: -8px;
`;
